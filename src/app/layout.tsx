import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Luan Omodei Marinheiro | Analista de Sistemas Jr",
  description: "Portfolio profissional de Luan Omodei Marinheiro - Analista de Sistemas Jr especializado em suporte técnico, desenvolvimento de soluções e implementação de melhorias de processos.",
  keywords: "Luan Omodei, Analista de Sistemas, Desenvolvimento, Programação, TI, Suporte Técnico, Portfolio",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className="scroll-smooth">
      <head>
        <link rel="icon" href="/favicon.ico" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
